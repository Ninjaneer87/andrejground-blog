import type { COLOR_PALETTE_OPTIONS, DARK_MODE_OPTIONS } from '../constants';

export type ColorPaletteOption = (typeof COLOR_PALETTE_OPTIONS)[number];
export type DarkModeOption = (typeof DARK_MODE_OPTIONS)[number];

